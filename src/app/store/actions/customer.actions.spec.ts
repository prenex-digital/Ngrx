import * as fromCustomer from './customer.actions';

describe('loadCustomers', () => {
  it('should return an action', () => {
    // expect(fromCustomer.addCustomer(customer).type).toBe('[Customer] Add Customer');
  });
});
